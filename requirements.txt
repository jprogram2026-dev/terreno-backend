# Backend Terreno — dependências
fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2          # Cliente HTTP async para proxy de APIs externas
pytrends==4.9.2        # Google Trends (scraping não-oficial)
urllib3<2.0            # Pin necessário — pytrends 4.9.2 usa API antiga de Retry
pydantic==2.9.2
python-dotenv==1.0.1
cachetools==5.5.0      # Cache em memória (substitui Redis para projeto acadêmico)
google-cloud-bigquery==3.26.0  # RAIS via Base dos Dados (BigQuery público)

# Opcional para CNPJ dump da Receita — descomente se for ingerir:
# psycopg2-binary==2.9.10
# sqlalchemy==2.0.36
